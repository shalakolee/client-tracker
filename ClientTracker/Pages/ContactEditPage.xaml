<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="ClientTracker.Pages.ContactEditPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:ClientTracker.Controls"
    xmlns:services="clr-namespace:ClientTracker.Services"
    Title="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Title_ContactEdit]}">

    <ContentPage.Resources>
        <services:InverseBoolConverter x:Key="InverseBoolConverter" />
    </ContentPage.Resources>

    <ContentPage.Background>
        <AppThemeBinding Light="{StaticResource AppBackgroundBrush}" Dark="{StaticResource AppBackgroundDarkBrush}" />
    </ContentPage.Background>

    <ContentPage.Behaviors>
        <controls:SwipeBackBehavior />
    </ContentPage.Behaviors>

    <Grid RowDefinitions="Auto,*">
        <controls:TopBar NavigationMode="Back"
            Title="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Title_ContactEdit]}"
            Subtitle="{Binding SelectedClient.Name}">
            <controls:TopBar.RightContent>
                <HorizontalStackLayout Spacing="8" VerticalOptions="Center">
                    <Button Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Action_Save]}" Command="{Binding SaveCommand}" Style="{StaticResource BaseButton}" />
                    <Button Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Action_Delete]}" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButton}" />
                </HorizontalStackLayout>
            </controls:TopBar.RightContent>
        </controls:TopBar>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="{StaticResource PagePadding}" Spacing="16">
            <Border BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource Gray950}}" StrokeThickness="0" Padding="16">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="20" />
            </Border.StrokeShape>
                <VerticalStackLayout Spacing="12">
                    <VerticalStackLayout Spacing="6">
                        <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Title_Clients]}" Style="{StaticResource MutedLabel}" />
                        <Border Style="{StaticResource FormFieldContainer}" IsVisible="{Binding CanSelectClient}">
                            <Picker
                                Title="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Title_Clients]}"
                                ItemsSource="{Binding Clients}"
                                ItemDisplayBinding="{Binding Name}"
                                SelectedItem="{Binding SelectedClient}"
                                Style="{StaticResource FormFieldPicker}" />
                        </Border>
                        <Border Style="{StaticResource FormFieldContainer}" IsVisible="{Binding CanSelectClient, Converter={StaticResource InverseBoolConverter}}">
                            <Label Text="{Binding SelectedClient.Name}" />
                        </Border>
                    </VerticalStackLayout>
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                        <VerticalStackLayout Spacing="6">
                            <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Contact_Name]}" Style="{StaticResource MutedLabel}" />
                            <Border Style="{StaticResource FormFieldContainer}">
                                <Entry Text="{Binding Name}" Style="{StaticResource FormFieldEntry}" />
                            </Border>
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Column="1" Spacing="6">
                            <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Contact_Phone]}" Style="{StaticResource MutedLabel}" />
                            <Border Style="{StaticResource FormFieldContainer}">
                                <Entry Keyboard="Telephone" Text="{Binding Phone}" Style="{StaticResource FormFieldEntry}" />
                            </Border>
                        </VerticalStackLayout>
                    </Grid>
                    <VerticalStackLayout Spacing="6">
                        <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Contact_Email]}" Style="{StaticResource MutedLabel}" />
                        <Border Style="{StaticResource FormFieldContainer}">
                            <Entry Keyboard="Email" Text="{Binding Email}" Style="{StaticResource FormFieldEntry}" />
                        </Border>
                    </VerticalStackLayout>
                    <VerticalStackLayout Spacing="6">
                        <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Contact_Notes]}" Style="{StaticResource MutedLabel}" />
                        <Border Style="{StaticResource FormFieldContainer}">
                            <Editor Text="{Binding Notes}" AutoSize="TextChanges" Style="{StaticResource FormFieldEditor}" />
                        </Border>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <Label Text="{Binding StatusMessage}" TextColor="{StaticResource Tertiary}" />
            </VerticalStackLayout>
        </ScrollView>
        <controls:LoadingOverlay Grid.RowSpan="2" ZIndex="99" />
    </Grid>
</ContentPage>





