<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="ClientTracker.Pages.ClientViewPage"
    x:Name="ClientViewPageRoot"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:ClientTracker.Controls"
    xmlns:services="clr-namespace:ClientTracker.Services"
    Title="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Title_Client]}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <services:InitialsConverter x:Key="InitialsConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Background>
        <AppThemeBinding Light="{StaticResource AppBackgroundBrush}" Dark="{StaticResource AppBackgroundDarkBrush}" />
    </ContentPage.Background>

    <ContentPage.Behaviors>
        <controls:SwipeBackBehavior />
    </ContentPage.Behaviors>

    <Grid RowDefinitions="Auto,*">
        <controls:TopBar NavigationMode="Back" Title="{Binding ClientName}" Subtitle="{Binding LocationLine}">
            <controls:TopBar.RightContent>
                <HorizontalStackLayout Spacing="8" VerticalOptions="Center">
                    <Button Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Action_AddSale]}" Command="{Binding AddSaleCommand}" Style="{StaticResource InlineButton}" />
                    <Button Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Action_Edit]}" Command="{Binding EditCommand}" Style="{StaticResource InlineButton}" />
                    <Button Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Action_Delete]}" Command="{Binding DeleteCommand}" Style="{StaticResource InlineDangerButton}" />
                </HorizontalStackLayout>
            </controls:TopBar.RightContent>
        </controls:TopBar>

        <RefreshView Grid.Row="1" Command="{Binding RefreshCommand}" IsRefreshing="{Binding IsBusy}">
            <ScrollView>
                <VerticalStackLayout Padding="{StaticResource PagePadding}" Spacing="18">
                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                    <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Title_Client]}" Style="{StaticResource SectionTitle}" />
                    <Button Grid.Column="1"
                            Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Action_Edit]}"
                            Command="{Binding EditCommand}"
                            Style="{StaticResource InlineButton}" />
                </Grid>
                <Border BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource Gray950}}" StrokeThickness="0" Padding="16">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="18" />
                    </Border.StrokeShape>
                    <VerticalStackLayout Spacing="14">
                        <VerticalStackLayout Spacing="4">
                            <Label Text="{Binding ClientName}" FontAttributes="Bold" FontSize="18" />
                            <Label Text="{Binding LocationLine}" Style="{StaticResource MutedLabel}" />
                        </VerticalStackLayout>
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="16">
                            <VerticalStackLayout Spacing="4">
                                <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Client_Address]}" Style="{StaticResource MutedLabel}" />
                                <Label Text="{Binding AddressLine1}" FontAttributes="Bold" LineBreakMode="TailTruncation" MaxLines="1" />
                                <Label Text="{Binding AddressLine2}" Style="{StaticResource MutedLabel}" LineBreakMode="TailTruncation" MaxLines="1" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Grid.Column="1" Spacing="4" HorizontalOptions="End">
                                <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Client_TaxId]}" Style="{StaticResource MutedLabel}" HorizontalTextAlignment="End" />
                                <Label Text="{Binding TaxId}" FontAttributes="Bold" HorizontalTextAlignment="End" LineBreakMode="TailTruncation" MaxLines="1" />
                            </VerticalStackLayout>
                        </Grid>
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="16">
                            <VerticalStackLayout Spacing="4">
                                <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Client_CityStatePostal]}" Style="{StaticResource MutedLabel}" />
                                <Label Text="{Binding CityStatePostalLine}" LineBreakMode="TailTruncation" MaxLines="1" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Grid.Column="1" Spacing="4" HorizontalOptions="End">
                                <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Client_Country]}" Style="{StaticResource MutedLabel}" HorizontalTextAlignment="End" />
                                <Label Text="{Binding Country}" HorizontalTextAlignment="End" LineBreakMode="TailTruncation" MaxLines="1" />
                            </VerticalStackLayout>
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <Border BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource Gray950}}" StrokeThickness="0">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="18" />
                    </Border.StrokeShape>
                    <VerticalStackLayout>
                        <Grid Padding="16,12" ColumnDefinitions="*,Auto">
                            <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Dashboard_TotalSales]}" Style="{StaticResource MutedLabel}" />
                            <Label Grid.Column="1" Text="{Binding SaleCount}" FontAttributes="Bold" />
                        </Grid>
                        <BoxView HeightRequest="1" BackgroundColor="{AppThemeBinding Light={StaticResource BorderSubtle}, Dark={StaticResource Gray600}}" Margin="16,0" />
                        <Grid Padding="16,12" ColumnDefinitions="*,Auto">
                            <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Sales_Column_Amount]}" Style="{StaticResource MutedLabel}" />
                            <Label Grid.Column="1" Text="{Binding TotalSaleAmount, StringFormat='{0:C}'}" FontAttributes="Bold" />
                        </Grid>
                        <BoxView HeightRequest="1" BackgroundColor="{AppThemeBinding Light={StaticResource BorderSubtle}, Dark={StaticResource Gray600}}" Margin="16,0" />
                        <Grid Padding="16,12" ColumnDefinitions="*,Auto">
                            <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Client_OutstandingCommission]}" Style="{StaticResource MutedLabel}" />
                            <Label Grid.Column="1" Text="{Binding OutstandingCommissionAmount, StringFormat='{0:C}'}" FontAttributes="Bold" />
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                    <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Client_Contacts]}" Style="{StaticResource SectionTitle}" />
                    <Button Grid.Column="1" Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Action_Add]}" Command="{Binding AddContactCommand}" Style="{StaticResource InlineButton}" />
                </Grid>
                <Border BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource Gray950}}" StrokeThickness="0">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="18" />
                    </Border.StrokeShape>
                    <CollectionView ItemsSource="{Binding Contacts}" SelectionMode="None">
                        <CollectionView.ItemsLayout>
                            <LinearItemsLayout Orientation="Vertical" ItemSpacing="0" />
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Grid RowDefinitions="Auto,1">
                                    <Grid Padding="16,12" ColumnDefinitions="Auto,*,Auto" RowDefinitions="Auto,Auto" ColumnSpacing="12" RowSpacing="4" MinimumHeightRequest="72">
                                        <Grid.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding BindingContext.ViewContactCommand, Source={x:Reference ClientViewPageRoot}}" CommandParameter="{Binding .}" />
                                        </Grid.GestureRecognizers>
                                        <Border WidthRequest="40" HeightRequest="40" BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceAlt}, Dark={StaticResource Gray900}}" StrokeThickness="0" VerticalOptions="Center" Grid.RowSpan="2">
                                            <Border.StrokeShape>
                                                <RoundRectangle CornerRadius="20" />
                                            </Border.StrokeShape>
                                            <Label Text="{Binding Name, Converter={StaticResource InitialsConverter}}" FontAttributes="Bold" FontSize="14" HorizontalOptions="Center" VerticalOptions="Center" />
                                        </Border>

                                        <Label Grid.Column="1" Text="{Binding Name}" FontAttributes="Bold" FontSize="16" LineBreakMode="TailTruncation" MaxLines="1" />
                                        <Label Grid.Column="1" Grid.Row="1" Text="{Binding Email}" Style="{StaticResource MutedLabel}" LineBreakMode="TailTruncation" MaxLines="1" />

                                        <Image Grid.Column="2"
                                               Grid.RowSpan="2"
                                               Source="{AppThemeBinding Light=icon_chevron_right.svg, Dark=icon_chevron_right_dark.svg}"
                                               WidthRequest="18"
                                               HeightRequest="18"
                                               Opacity="0.6"
                                               VerticalOptions="Center" />
                                    </Grid>
                                    <BoxView Grid.Row="1" HeightRequest="1" BackgroundColor="{AppThemeBinding Light={StaticResource BorderSubtle}, Dark={StaticResource Gray600}}" Margin="72,0,16,0" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Border>

                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                    <Label Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Client_Sales]}" Style="{StaticResource SectionTitle}" />
                    <Button Grid.Column="1" Text="{Binding Source={x:Static services:LocalizationResourceManager.Instance}, Path=[Action_AddSale]}" Command="{Binding AddSaleCommand}" Style="{StaticResource InlineButton}" />
                </Grid>
                <Border BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource Gray950}}" StrokeThickness="0">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="18" />
                    </Border.StrokeShape>
                    <CollectionView ItemsSource="{Binding Sales}" SelectionMode="None">
                        <CollectionView.ItemsLayout>
                            <LinearItemsLayout Orientation="Vertical" ItemSpacing="0" />
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Grid RowDefinitions="Auto,1">
                                    <Grid Padding="16,12" ColumnDefinitions="*,Auto,Auto" RowDefinitions="Auto,Auto" ColumnSpacing="12" RowSpacing="4" MinimumHeightRequest="72">
                                        <Grid.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding BindingContext.ViewSaleCommand, Source={x:Reference ClientViewPageRoot}}" CommandParameter="{Binding .}" />
                                        </Grid.GestureRecognizers>
                                        <Label Text="{Binding InvoiceNumber, StringFormat='Invoice {0}'}" FontAttributes="Bold" FontSize="16" LineBreakMode="TailTruncation" MaxLines="1" />
                                        <Label Grid.Row="1" Text="{Binding ContactName}" Style="{StaticResource MutedLabel}" LineBreakMode="TailTruncation" MaxLines="1" />

                                        <VerticalStackLayout Grid.Column="1" Grid.RowSpan="2" Spacing="2" HorizontalOptions="End" VerticalOptions="Center">
                                            <Label Text="{Binding Amount, StringFormat='{0:C}'}" FontAttributes="Bold" HorizontalTextAlignment="End" />
                                            <Label Text="{Binding SaleDate, StringFormat='{0:d}'}" Style="{StaticResource MutedLabel}" HorizontalTextAlignment="End" />
                                        </VerticalStackLayout>
                                        <Image Grid.Column="2"
                                               Grid.RowSpan="2"
                                               Source="{AppThemeBinding Light=icon_chevron_right.svg, Dark=icon_chevron_right_dark.svg}"
                                               WidthRequest="18"
                                               HeightRequest="18"
                                               Opacity="0.6"
                                               VerticalOptions="Center" />
                                    </Grid>
                                    <BoxView Grid.Row="1" HeightRequest="1" BackgroundColor="{AppThemeBinding Light={StaticResource BorderSubtle}, Dark={StaticResource Gray600}}" Margin="16,0" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Border>

                <Label Text="{Binding StatusMessage}" TextColor="{StaticResource Tertiary}" />
                </VerticalStackLayout>
            </ScrollView>
        </RefreshView>
        <controls:LoadingOverlay Grid.RowSpan="2" ZIndex="99" />
    </Grid>
</ContentPage>


